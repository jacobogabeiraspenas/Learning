from manim import config

# Set lower FPS
config.frame_rate = 5  # Set to desired FPS

# Set quality options
config.pixel_height = 270  # Lower vertical resolution (default is 1080)
config.pixel_width = 480   # Lower horizontal resolution (default is 1920)
config.frame_height = 2.0  # Lower frame height (default is 8.0)
config.frame_width = 3.55   # Lower frame width (default is 14.2)
